{
  "name": "kuccps-career-hub",
  "version": "1.0.0",
  "description": "KUCCPS Career Hub - Vercel Project",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
